# Python SCA Vulnerabilities - Known Vulnerable Dependencies
# This file intentionally contains outdated packages with known CVEs

# Django with multiple CVEs (very old version)
Django==2.2.0

# Flask with known vulnerabilities
Flask==0.12.2

# Requests with security issues
requests==2.6.0

# PyYAML with arbitrary code execution vulnerability (CVE-2020-1747)
PyYAML==5.1

# Jinja2 with XSS vulnerabilities
Jinja2==2.10

# Pillow with multiple image processing vulnerabilities
Pillow==6.0.0

# Cryptography with various security issues
cryptography==2.3

# SQLAlchemy with SQL injection risks
SQLAlchemy==1.2.0

# Werkzeug with security vulnerabilities
Werkzeug==0.14.0

# urllib3 with multiple CVEs
urllib3==1.24

# Pycryptodome with cryptographic weaknesses
pycryptodome==3.6.0

# lxml with XXE vulnerabilities
lxml==4.3.0

# httplib2 with security issues
httplib2==0.12.0

# paramiko with authentication bypass
paramiko==2.4.0

# ansible with multiple security issues
ansible==2.7.0
